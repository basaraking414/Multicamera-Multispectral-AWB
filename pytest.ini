[pytest]
# pytest 配置文件
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 输出格式
addopts = -v --tb=short

# 标记定义
markers =
    slow: 标记慢速测试
    gpu: 标记需要GPU的测试
